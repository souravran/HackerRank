#include <stdlib.h>
#include <stdio.h>

int main() {
  
     // Complete the code.
    int it;
    long lg;
    long long llg;
    char ch;
    float ft;
    double d;
    scanf("%d %ld %lld %c %f %lf", &it, &lg, &llg, &ch, &ft, &d);
    
    printf("%d\n%ld\n%lld\n%c\n%f\n%lf", it, lg, llg, ch, ft, d);

    return 0;
}
